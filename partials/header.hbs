
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" style="" href="{{@site.url}}">{{@site.title}}</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        {{#if @site.navigation}}
			{{navigation}}
		{{/if}}
		
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{t "Tags More"}}<span class="caret"></span></a>
          <ul class="dropdown-menu">
 			{{#get "tags" limit="all" include="count.posts" order="count.posts desc" }}
			{{#foreach tags}}
			<li class="nav-item" ><a href="/tag/{{slug}}" class="tag-item">{{name}} ({{count.posts}})</a></li>
			{{/foreach}}
			{{/get}}
          </ul>
        </li>
        {{#if @site.secondary_navigation}}
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{t "Nav More"}} 
					<span class="caret"></span>
			</a>
			<ul class="dropdown-menu">
				{{navigation type="secondary"}} 
			</ul>
		</li>
		{{/if}}
      </ul>
    </div>
  </div>
</nav>
<style>
.navbar-brand:after{
	content:"{{@site.title}}" !important;
 }
</style>
